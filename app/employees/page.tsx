import SoftwareLayout from "@/components/SoftwareLayout";

export default function EmployeesPage() {
  return (
    <SoftwareLayout>
      {/* Employees / Staff Page UI - to be added later */}
    </SoftwareLayout>
  );
}
